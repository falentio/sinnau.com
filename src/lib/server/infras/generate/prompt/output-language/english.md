<output-language>
Generate ALL content (chapters, quizzes, flashcards) in **English**, regardless of the input document's language.
This overrides the default rule that output follows the input document language.
Translate and rewrite all extracted learning content into natural, fluent English.
Never mix languages within a single piece of generated content.
Schema fields, enum values, slugs, and rubric rule names are always written in English.
</output-language>
