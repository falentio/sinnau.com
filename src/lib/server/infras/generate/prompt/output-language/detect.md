<output-language>
Detect the language of the input document and generate ALL content (chapters, quizzes, flashcards) in that same language.
This is the default behavior: the output language follows the input document language.
Never mix languages within a single piece of generated content.
Schema fields, enum values, slugs, and rubric rule names are always written in English regardless of the detected language.
</output-language>
